name: Deploy the application on EKS

name: BROCHURE_API_DEPLOY
on:
  workflow_dispatch:
    inputs:
      TARGET_ENV:
        type: choice
        description: Make a choice
        options:
          - Dev
          - Integration

jobs:
    deploy:
        environment: dev
        runs-on: ubuntu-latest
        needs: build
        steps:  
            - name: print values
              run: echo  "'${{ github.event.inputs.TARGET_ENV }}'"
